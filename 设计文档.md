# Share Helper (分享助手) 系统设计与开发文档

## 1. 项目概述

**Share Helper（分享助手）** 是一款旨在解决信息过载与碎片化分享痛点的智能工具。它能够自动接收并解析朋友跨平台分享的内容（文本、图片、网页链接、富媒体平台等），通过大语言模型（LLM）提取主题、概要，评估重要性、紧急程度，并预估阅读/观看时长。结合历史记忆关联与稍后阅读提醒功能，为用户提供高效的个人信息管家服务。

---

## 2. 系统架构设计（分布式演进）

为满足高可用和后续的平台扩展（Web -> 小程序/App），系统采用前后端分离与微服务化的分布式架构设计。

- **接入网关层 (API Gateway):** 基于 **FastAPI** 构建，负责处理 HTTP 请求、鉴权、限流以及初步的路由分发。
    
- **任务调度中心 (Message Queue):** 考虑到音视频链接解析可能耗时较长，引入消息队列（如 Redis Queue / RabbitMQ / Celery），实现异步非阻塞处理。
    
- **智能体引擎层 (Agent Engine):** 核心大脑，基于 **LangGraph** 或 **Dify** 编排工作流。
    
- **解析工具微服务 (Parser Services):** 独立部署的爬虫与解析节点，专门应对不同平台的反爬与数据结构。
    
- **存储层 (Storage):**
    
    - 关系型数据库 (PostgreSQL): 存储用户数据、分享记录、提醒任务。
        
    - 向量数据库 (Milvus / Qdrant): 存储解析内容的向量化表征，用于“记忆”检索。
        

---

## 3. 重点要求深入设计与实现逻辑

### 3.1 明确的智能体范式 (Agent Paradigm: Tool Use / ReAct)

为应对复杂多变的分享输入，系统采用 **Tool Use (函数调用)** 结合 **ReAct (Reasoning and Acting)** 的智能体范式。这里推荐使用 **LangGraph**，因为它能够极好地管理循环状态和多步推理。

**工作流设计 (LangGraph State Graph):**

1. **输入分析节点 (Input Classifier):** 接收用户输入。LLM 判断输入类型（纯文本、图片、含URL的文本）。
    
2. **工具调用节点 (Tool Executor):** LLM 根据判断结果选择合适的 Tool：
    
    - `ZhihuParserTool`: 调用知乎接口或无头浏览器抓取正文。
        
    - `BilibiliParserTool`: 提取B站视频字幕、简介、时长。
        
    - `DouyinParserTool`: 获取抖音视频文案和关键帧。
        
    - `WeChatArticleTool`: 解析微信公众号文章正文。
        
    - `ImageOCRTool`: 调用 OCR 接口提取图片文字。
        
3. **记忆检索节点 (Memory Retriever):** 在向量库中搜索与当前内容相似的历史分享。
    
4. **综合分析节点 (Analyzer):** LLM 接收解析后的原始内容和历史记忆，输出最终的 JSON 结构：
    
    JSON
    
    ```
    {
      "theme": "人工智能",
      "summary": "介绍了vLLM加速大模型推理的原理...",
      "importance": 8,
      "urgency": 3,
      "estimated_time_minutes": 15,
      "related_past_shares": ["上周张三分享的TensorRT教程"]
    }
    ```
    

### 3.2 大模型集成模式 (双模兼容)

系统架构必须将 LLM 调用层抽象化，以实现云端 API 和本地部署的无缝切换。可使用 LangChain 的 `ChatOpenAI` 封装器配合兼容 OpenAI 格式的网关（如 LiteLLM 或 OneAPI）。

- **闭源 API 模式:** 配置环境变量指向外部 API（如 OpenAI GPT-4o、通义千问、智谱 GLM-4 等），适合冷启动和轻量化运营。
    
- **本地部署模式:** 配置本地服务地址。系统在处理涉密或极度隐私的个人分享内容时，可动态路由至本地模型进行推理。
    

### 3.3 本地大模型推理加速优化 (基于 vLLM)

当系统启用本地开源大模型（如 Qwen-2.5-7B/14B, Llama-3）时，直接使用原生 HuggingFace `transformers` 性能极低。必须作为独立微服务使用 **vLLM** 进行部署优化：

- **PagedAttention (核心 KV Cache 优化):** 分享助手在进行长文本（如万字长文、长视频字幕）摘要时，会产生大量 Key-Value (KV) 缓存。vLLM 的 PagedAttention 能够将 KV Cache 划分成块，像操作系统管理内存页一样管理显存，极大减少显存碎片，将批处理吞吐量提升数倍。
    
- **模型量化 (Quantization):** 为降低分布式部署的硬件成本，使用 **AWQ** 或 **GPTQ** 量化算法。在加载模型时，通过 vLLM 参数 `--quantization awq` 将模型权重降至 4-bit，实现在单张消费级显卡（如 RTX 4090）或中端计算卡上运行 14B+ 参数模型，且精度损失极小。
    
- **Continuous Batching:** 当同一时间收到多个好友的分享时，vLLM 的连续批处理能力可以在微秒级动态插入新请求，无需等待当前批次所有任务完成，极大降低请求延迟。
    
- **部署命令示例:**
    
    Bash
    
    ```
    python -m vllm.entrypoints.openai.api_server \
        --model Qwen/Qwen2.5-7B-Instruct-AWQ \
        --quantization awq \
        --tensor-parallel-size 1 \
        --max-model-len 8192 \
        --gpu-memory-utilization 0.9
    ```
    

### 3.4 容器化与 K8s 分布式部署设计

为满足后续服务化、高可用及横向扩展需求，所有组件必须容器化，并托管至 Kubernetes (K8s) 集群中。

**K8s 核心资源设计:**

1. **Ingress Controller:** 处理外部域名访问，将 Web 前端请求路由至 FastAPI。
    
2. **FastAPI Gateway Deployment:**
    
    - 配置 **HPA (Horizontal Pod Autoscaler)**：根据 CPU/内存使用率自动扩缩容，应对突发的分享高峰。
        
3. **LangGraph Worker Deployment:** * 独立部署的异步消费节点。监听消息队列中的待处理任务。
    
4. **vLLM Inference StatefulSet/Deployment:**
    
    - 使用 K8s 的 **Device Plugin for GPUs**，将节点上的 GPU 资源分配给 vLLM Pods。
        
    - 可配置多个副本，通过内部 Service 实现负载均衡。
        
5. **Headless Service 配合工具节点:** 针对爬虫类工具，由于容易被封 IP，可在 K8s 中部署具有动态代理池的爬虫 Pod 集群。
    

---

## 4. 附加功能设计：记忆与提醒

- **记忆功能 (Memory Merge):** 解析完成后，将“主题+摘要”通过 Embedding 模型转化为向量存入 Milvus。当新链接进入时，先进行 Top-K 检索。如果相似度超过阈值（如 0.85），则在 Agent 的 Prompt 中注入：“_系统提示：用户的朋友之前分享过内容 A，请在分析本次分享 B 时，指出两者的关联与演进。_”
    
- **日程提醒 (Reminder Time):** FastAPI 集群中集成 `APScheduler` 或结合外部的系统级定时任务服务。当 Agent 评估该内容耗时较长（例如 > 20分钟）且当前为工作时间时，提示用户“是否设为稍后阅读”。用户确认后，将任务写入数据库，并在指定时间通过 Webhook 或长连接推送到前端。
    

---

## 5. 开发路径与迭代计划

- **Phase 1 (MVP Web 版):** 完成 FastAPI 基础骨架，LangGraph 流程跑通。集成基础 WebScraper 和 1-2 个国内平台专用 Parser。使用外部大模型 API 进行跑通测试，提供基础的 Web 交互界面输入链接并展示分析报告。
    
- **Phase 2 (分布式改造与本地化):** 引入 vLLM 部署本地模型。拆分 Agent 逻辑至 Celery/RabbitMQ 异步队列。编写 Dockerfile 与 K8s YAML 文件，完成集群部署测试。
    
- **Phase 3 (多端触达与记忆深化):** 接入向量数据库完善记忆合并功能。开发微信小程序端/APP端，支持直接通过系统“分享”菜单将内容传递给助手。