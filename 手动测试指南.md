# ShareConn 手动部署与测试指南

> 本文档面向将项目工程文件迁移至新设备后，从零启动并完成端到端功能验证的完整流程。

---

## 一、环境前置要求

在新设备上确保已安装以下软件：

| 软件 | 最低版本 | 用途 | 安装参考 |
|------|----------|------|----------|
| **Docker Desktop** | 24.0+ | 运行所有后端服务（含数据库） | https://docs.docker.com/desktop/ |
| **Docker Compose** | v2（Docker Desktop 自带） | 容器编排 | Docker Desktop 默认包含 |
| **Node.js** | 18.0+ | 前端开发服务器 | https://nodejs.org/ |
| **npm** | 9.0+（随 Node.js 安装） | 前端依赖管理 | 随 Node.js 自动安装 |
| **Git** | 可选 | 版本控制 | https://git-scm.com/ |

### 快速检查命令

```powershell
docker --version        # Docker version 24.x+
docker compose version  # Docker Compose version v2.x+
node --version          # v18.x+
npm --version           # 9.x+
```

---

## 二、项目结构概览

```
ShareConn/
├── app/                    # Python 后端（FastAPI + LangGraph + Celery）
│   ├── agent/              # LangGraph Agent 工作流
│   ├── api/                # REST API 端点
│   ├── config.py           # 全局配置
│   ├── db/                 # 数据库连接
│   ├── llm/                # LLM 提供者工厂
│   ├── memory/             # 向量记忆检索
│   ├── models/             # SQLAlchemy ORM 模型
│   ├── schemas/            # Pydantic 请求/响应模型
│   ├── tasks/              # Celery 异步任务
│   └── tools/              # 平台解析器（微信、B站等）
├── frontend/               # Vue 3 + Vite 前端
├── docker-compose.yml      # 容器编排
├── Dockerfile              # Python 应用镜像
├── .env                    # 环境变量（⚠️ 需要配置）
├── seed_test_user.py       # 测试用户初始化脚本
└── pyproject.toml          # Python 依赖定义
```

---

## 三、配置环境变量

### 3.1 编辑 `.env` 文件

项目根目录下的 `.env` 文件是整个系统的核心配置。**在新设备上必须检查并修改以下关键项**：

```ini
# ===== 应用 =====
APP_NAME="Share Helper"
APP_ENV=development
DEBUG=true

# ===== 数据库 =====
# ⚠️ 注意：此处 host 必须是 "postgres"（Docker 容器服务名），不是 "localhost"
DATABASE_URL="postgresql+asyncpg://postgres:password@postgres:5432/sharehelper"

# ===== Redis =====
# ⚠️ 同理，host 是 "redis"（容器服务名）
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ===== LLM - 云端 (阿里云 DashScope) =====
CLOUD_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
CLOUD_LLM_API_KEY=sk-你的阿里云API密钥    # ⚠️ 必须替换为有效的 API Key
CLOUD_LLM_MODEL=qwen3-30b-a3b              # 或其他 DashScope 支持的模型

# ===== LLM - 本地 (可选) =====
LOCAL_LLM_ENABLED=false

# ===== 向量数据库 =====
# ⚠️ host 是 "qdrant"（容器服务名）
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION=share_memories
EMBEDDING_MODEL=text-embedding-v3           # 阿里云向量模型
MEMORY_SIMILARITY_THRESHOLD=0.85

# ===== 安全 =====
SECRET_KEY=change-me-in-production-use-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=10080
```

> **⚠️ 重点注意**：
> - `DATABASE_URL`、`REDIS_URL`、`QDRANT_URL` 中的 host 必须使用 **Docker 服务名**（`postgres`、`redis`、`qdrant`），**不要**使用 `localhost`。
> - `CLOUD_LLM_API_KEY` 必须替换为你自己的有效阿里云 DashScope API Key。
> - `EMBEDDING_MODEL` 必须使用 `text-embedding-v3`（阿里云支持），**不要**用 OpenAI 的 `text-embedding-3-small`。

---

## 四、启动后端服务（Docker Compose）

### 4.1 构建并启动所有容器

```powershell
cd ShareConn

# 首次启动需要构建镜像（约 1-3 分钟）
docker compose up -d --build
```

这会启动以下 5 个服务：

| 容器名 | 服务 | 端口映射 | 说明 |
|--------|------|----------|------|
| `shareconn-app-1` | FastAPI 应用 | `8000:8000` | REST API + WebSocket |
| `shareconn-celery-worker-1` | Celery Worker | 无外部端口 | 异步任务处理 |
| `shareconn-postgres-1` | PostgreSQL 16 | `5433:5432` | 主数据库 |
| `shareconn-redis-1` | Redis 7 | `6379:6379` | 消息队列 |
| `shareconn-qdrant-1` | Qdrant | `6333:6333` | 向量数据库 |

### 4.2 验证容器状态

```powershell
docker compose ps
```

**期望输出**：所有 5 个容器 `STATUS` 显示为 `Up`。

### 4.3 查看日志（排查问题用）

```powershell
# 查看所有服务日志
docker compose logs -f

# 只看应用日志
docker logs -f shareconn-app-1

# 只看 Worker 日志
docker logs -f shareconn-celery-worker-1
```

---

## 五、初始化测试数据

### 5.1 创建数据库表

FastAPI 应用启动时会自动通过 SQLAlchemy ORM 创建所有必要的表。等待 `shareconn-app-1` 完全启动后（约 5-10 秒），验证数据库：

```powershell
docker exec shareconn-postgres-1 psql -U postgres -d sharehelper -c "\dt"
```

**期望输出**：应该看到 `users`、`shares`、`reminders` 等表。

### 5.2 初始化测试用户

**⚠️ 这一步是必须的！** API 端点需要用户认证，必须先创建测试用户。

```powershell
docker exec shareconn-app-1 uv run python seed_test_user.py
```

**期望输出**：
```
User created.
TEST_TOKEN=eyJhbGciOiJIUzI1NiIs...（一长串 JWT Token）
```

> **🔑 请复制 `TEST_TOKEN=` 后面的整段 Token 值**，后面配置前端时需要使用。
>
> 如果看到 `User already exists.`，说明用户已存在，脚本依然会输出有效 Token。

---

## 六、验证后端 API

在初始化用户之后，逐步验证每个 API 端点。

### 6.1 健康检查

```powershell
# 根路径
Invoke-RestMethod -Uri http://127.0.0.1:8000/

# 健康检查端点
Invoke-RestMethod -Uri http://127.0.0.1:8000/api/v1/health
```

**期望**：返回包含 `APP_NAME` 和 `status: ok` 的 JSON。

### 6.2 测试分享提交 API

```powershell
$token = "你在步骤5.2中获取的Token"

$headers = @{
    "Authorization" = "Bearer $token"
    "Content-Type"  = "application/json"
}

$body = '{"content": "https://mp.weixin.qq.com/s/eUbRoyKxOjiAPuXZi8DPsQ", "input_type": "url"}'

Invoke-RestMethod -Uri http://127.0.0.1:8000/api/v1/shares -Method POST -Headers $headers -Body $body
```

**期望输出**：
```json
{
    "share_id": 1,
    "status": "pending",
    "message": "分享已提交，正在处理中"
}
```

### 6.3 等待处理并查询结果

提交后耐心等待约 **7-15 秒**（取决于网络和 LLM 响应速度），然后查询结果：

```powershell
$shareId = 1  # 替换为上一步返回的 share_id

Invoke-RestMethod -Uri "http://127.0.0.1:8000/api/v1/shares/$shareId" -Headers $headers
```

**期望输出**：`status` 字段从 `pending` → `processing` → `completed`，且 `theme`、`summary`、`importance` 等分析字段有值。

### 6.4 查看 Celery Worker 日志验证处理流程

处理过程中可在另一终端观察：

```powershell
docker logs -f shareconn-celery-worker-1
```

**正常的日志应依次出现**：
1. `[Celery] 开始处理分享: share_id=1`
2. `[分类节点] 规则匹配: platform=wechat`
3. `[解析节点] 解析完成: title=...`
4. `[记忆节点] 开始检索相关历史分享`
5. `[分析节点] 分析完成: theme=..., importance=...`
6. `记忆已存储: share_id=1`
7. `[Celery] 分享处理完成: share_id=1`

---

## 七、启动前端页面

### 7.1 安装前端依赖

```powershell
cd frontend
npm install
```

### 7.2 配置前端 Token

编辑 `frontend/src/api.js`，将 `Authorization` 头中的 Token 替换为你在步骤 5.2 中获取的值：

```javascript
const api = axios.create({
  baseURL: 'http://127.0.0.1:8000/api/v1',
  headers: {
    'Authorization': 'Bearer 你的TEST_TOKEN'  // ← 替换这里
  },
  timeout: 60000,
});
```

同时确认 WebSocket 地址正确：

```javascript
export const createProgressSocket = (shareId, onMessage) => {
  const ws = new WebSocket(`ws://127.0.0.1:8000/ws/shares/${shareId}`);
  // ...
};
```

> **注意**：API 和 WebSocket 的端口都是 `8000`，对应 Docker 映射的 FastAPI 端口。

### 7.3 启动前端开发服务器

```powershell
npm run dev
```

**期望输出**：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

### 7.4 打开浏览器

访问 **http://localhost:5173**，应看到 "Share Helper" 页面。

---

## 八、前端端到端测试

### 8.1 提交链接测试

1. 在输入框中粘贴一个 URL，例如：
   ```
   https://mp.weixin.qq.com/s/eUbRoyKxOjiAPuXZi8DPsQ
   ```
2. 点击提交按钮。
3. 页面应依次展示：
   - **"正在处理中"** + 加载动画
   - 约 7-15 秒后自动切换为 **分析结果卡片**，包含：
     - 平台标识（微信公众号）
     - 文章标题
     - AI 智能摘要
     - 重要性评分 / 紧急程度
     - 预估阅读时长
     - 历史记忆关联（首次为空或 LLM 自行推断）

### 8.2 验证哪些功能可以测试

| 功能 | 测试方式 | 预期结果 |
|------|----------|----------|
| 微信文章解析 | 提交 `mp.weixin.qq.com` 链接 | 标题、作者、正文提取正确 |
| B站视频解析 | 提交 `bilibili.com/video/BVxxxx` 链接 | 视频标题、UP主、简介提取正确 |
| 通用网页解析 | 提交任意网页 URL | 基本正文提取 |
| LLM 内容分析 | 提交任意链接并等待结果 | 主题、摘要、重要性评分生成 |
| 向量记忆存储 | 提交两个相似主题的链接 | 第二次提交时日志显示"找到 N 条相关记忆" |

---

## 九、常见问题排查

### Q1: 容器启动失败

```powershell
# 查看完整错误日志
docker compose logs app
docker compose logs celery-worker
```

常见原因：
- `.env` 文件中 `DATABASE_URL` 格式错误
- Docker Desktop 未启动

### Q2: API 返回 404 "User not found"

原因：未执行 `seed_test_user.py` 初始化测试用户。

```powershell
docker exec shareconn-app-1 uv run python seed_test_user.py
```

### Q3: Celery Worker 报 `attached to a different loop`

原因：旧版本代码问题。确认 `app/tasks/share_processing.py` 中使用了 `asyncio.new_event_loop()` 和 `_get_fresh_db()` 模式，而非共享全局 engine。

解决：重新构建镜像：
```powershell
docker compose down
docker compose up -d --build
```

### Q4: LLM 调用报错 `enable_thinking must be set to false`

原因：DashScope 兼容模式下，带思考模式的 Qwen 模型在非流式调用时需显式关闭。

确认 `app/llm/provider.py` 中有：
```python
model_kwargs={"extra_body": {"enable_thinking": False}},
```

### Q5: Embedding 报错 `contents is neither str nor list of str`

原因：LangChain 默认会将文本预处理为 Token ID 列表，但 DashScope 只接受纯字符串。

确认 `app/memory/embedding.py` 中有：
```python
check_embedding_ctx_length=False,
```

### Q6: 向量存储报 `expected dim: 1536, got 1024`

原因：旧的 Qdrant Collection 是按 OpenAI 的 1536 维创建的，但现在使用的 DashScope `text-embedding-v3` 输出 1024 维。

解决：删除旧集合，让程序自动重建：
```powershell
Invoke-RestMethod -Uri http://localhost:6333/collections/share_memories -Method DELETE
docker compose restart celery-worker
```

### Q7: 前端一直显示"等待处理..."

可能原因：
1. **Celery Worker 处理报错** — 查看 Worker 日志
2. **前端 Token 过期/无效** — 重新执行 `seed_test_user.py` 获取新 Token
3. **前端代码版本不一致** — 浏览器 `Ctrl+Shift+R` 强制刷新

---

## 十、停止和清理

### 停止所有服务（保留数据）

```powershell
docker compose down
```

### 停止并清除所有数据（包括数据库）

```powershell
docker compose down -v   # -v 会删除 Docker Volume（数据库数据、向量数据等）
```

### 完全清理（包括镜像）

```powershell
docker compose down -v --rmi all
```

---

## 十一、快速启动 Cheatsheet

```powershell
# === 1. 一键启动所有后端服务 ===
cd ShareConn
docker compose up -d --build

# === 2. 等待容器就绪后初始化测试用户 ===
Start-Sleep 15
docker exec shareconn-app-1 uv run python seed_test_user.py
# 📋 复制输出的 TEST_TOKEN

# === 3. 将 Token 写入前端配置 ===
# 编辑 frontend/src/api.js，替换 Authorization 头中的 Token

# === 4. 启动前端 ===
cd frontend
npm install
npm run dev

# === 5. 打开浏览器 ===
# 访问 http://localhost:5173
# 粘贴链接提交测试
```
